# ContextClaw TikTok Promo

Use the generated vertical asset:

- `docs/assets/contextclaw-promo.mp4`
- `docs/assets/contextclaw-promo.gif`

Generate them with:

```bash
python3 scripts/render_contextclaw_promo.py
```

## Hook

Use this text in the first three seconds:

> Your agents should not forget everything after one chat.

## Voiceover

```text
ContextClaw gives you deep-agent style building blocks without heavyweight lock-in.
You get real tools, MCP registry loading, task delegation to sub-agents, and session checkpoints.
Then ContextGraph makes those agents complementary with shared memory, discovery, and trust.
One agent researches, one codes, one governs memory.
That means safer automation, better handoffs, and agents that keep useful context instead of restarting from zero every session.
```

## On-screen captions

1. `Create role-specific agents`
2. `Use deep-agent style tools`
3. `Delegate work with task`
4. `Load MCP tools instantly`
5. `Resume from checkpoints`
6. `Connect shared memory with ContextGraph`

## Caption suggestion

```text
ContextClaw runs the agents. ContextGraph shares the memory.

Multi-agent runtime
Deep-agent style tools
MCP registry
Task delegation
Session checkpoints
Role-based policy
Shared memory and discovery

#aiagents #opensource #developertools #mcp #contextengineering
```
